# Ön yüzün derleme zamanı ayarı kalmadı: TURN kimlik bilgileri artık
# backend'den çalışma anında geliyor (GET /api/ice).
#
# Bu dosya, ileride bir VITE_ değişkeni gerekirse diye duruyor. TURN
# yapılandırması için backend/appsettings.json içindeki "Ice" bölümüne
# ve README'nin "Sesli/görüntülü görüşme" başlığına bakın:
#
#   export Ice__MeteredApiKey="metered panelindeki API anahtarı"
